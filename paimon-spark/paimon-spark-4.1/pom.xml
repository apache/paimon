<?xml version="1.0" encoding="UTF-8"?>
<!--
Licensed to the Apache Software Foundation (ASF) under one
or more contributor license agreements.  See the NOTICE file
distributed with this work for additional information
regarding copyright ownership.  The ASF licenses this file
to you under the Apache License, Version 2.0 (the
"License"); you may not use this file except in compliance
with the License.  You may obtain a copy of the License at

  http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing,
software distributed under the License is distributed on an
"AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
KIND, either express or implied.  See the License for the
specific language governing permissions and limitations
under the License.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.apache.paimon</groupId>
        <artifactId>paimon-spark</artifactId>
        <version>2.1-SNAPSHOT</version>
    </parent>

    <artifactId>paimon-spark-4.1_2.13</artifactId>
    <name>Paimon : Spark : 4.1 : 2.13</name>

    <properties>
        <spark.version>4.1.2</spark.version>
        <!-- Spark 4.x uses SLF4J 2.x (`org.slf4j.spi.LoggingEventBuilder` is 2.x-only). Override
             the Paimon parent pom's default `slf4j.version=1.7.32` so the classpath is uniformly
             2.x; otherwise `log4j-slf4j2-impl` + `slf4j-api:1.7.32` mix produces
             `NoClassDefFoundError: org/slf4j/spi/LoggingEventBuilder` at test startup. -->
        <slf4j.version>2.0.16</slf4j.version>
        <!-- Spark 4 only supports Scala 2.13. Pin it locally so this _2.13 module resolves its
             dependencies to _2.13 even when the spark4 profile is inactive (see issue #6682);
             otherwise it inherits scala.binary.version=2.12 from paimon-parent. -->
        <scala.binary.version>2.13</scala.binary.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.apache.paimon</groupId>
            <artifactId>paimon-format</artifactId>
        </dependency>

        <dependency>
            <groupId>org.apache.paimon</groupId>
            <artifactId>paimon-spark4-common_${scala.binary.version}</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.apache.paimon</groupId>
            <artifactId>paimon-spark-common_${scala.binary.version}</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!--
          `paimon-spark4-common` transitively pulls in `spark-sql-api` at the version set by
          `paimon-spark-common.spark.version` (4.2.0 under the `spark4` profile). Spark 4.2's
          `SqlApiConf` added abstract `singleCharacterPipeOperatorEnabled()`, which Spark 4.1's
          concrete anonymous subclasses (e.g. `WithTestConf$$anon$4`) don't implement. Mixing those
          jars produces `AbstractMethodError` at parser configuration time. Pin `spark-sql-api` to
          `${spark.version}` (4.1.2) so the classpath is uniformly 4.1 — the same pin
          `paimon-spark-4.0` carries for the same reason.
        -->
        <dependency>
            <groupId>org.apache.spark</groupId>
            <artifactId>spark-sql-api_${scala.binary.version}</artifactId>
            <version>${spark.version}</version>
            <exclusions>
                <!-- Spark ships a tiny `spark-connect-shims` jar carrying a stub
                     `org.apache.spark.SparkConf` with only `getAll()`. scalac resolves that in
                     preference to spark-core's real `SparkConf` and fails with
                     `value set is not a member of org.apache.spark.SparkConf`. The shims jar only
                     matters to Spark Connect clients. Same exclusion as `paimon-spark-4.0`. -->
                <exclusion>
                    <groupId>org.apache.spark</groupId>
                    <artifactId>spark-connect-shims_${scala.binary.version}</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>org.apache.spark</groupId>
            <artifactId>spark-sql_${scala.binary.version}</artifactId>
            <version>${spark.version}</version>
        </dependency>

        <dependency>
            <groupId>org.apache.spark</groupId>
            <artifactId>spark-core_${scala.binary.version}</artifactId>
            <version>${spark.version}</version>
        </dependency>

        <dependency>
            <groupId>org.apache.spark</groupId>
            <artifactId>spark-catalyst_${scala.binary.version}</artifactId>
            <version>${spark.version}</version>
        </dependency>

        <dependency>
            <groupId>org.apache.spark</groupId>
            <artifactId>spark-hive_${scala.binary.version}</artifactId>
            <version>${spark.version}</version>
        </dependency>

        <!-- test -->

        <!-- The 4.1-compiled build of `paimon-spark-ut`'s test sources. The baseline build cannot
             be used here: Spark 4.2 turned `QueryTest` into a trait (SPARK-55910), so a
             4.2-compiled `PaimonSparkTestBase` carries it as an interface and fails to load on a
             4.0/4.1 runtime. `paimon-spark-ut-4.0` does not fit either — test sources construct
             Spark internals directly, and `CTERelationDef` gained a 6th constructor argument in
             4.1. See `paimon-spark-ut-4.1/pom.xml`. -->
        <dependency>
            <groupId>org.apache.paimon</groupId>
            <artifactId>paimon-spark-ut-4.1_${scala.binary.version}</artifactId>
            <version>${project.version}</version>
            <classifier>tests</classifier>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>*</groupId>
                    <artifactId>*</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>org.apache.paimon</groupId>
            <artifactId>paimon-spark4-common_${scala.binary.version}</artifactId>
            <version>${project.version}</version>
            <classifier>tests</classifier>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>*</groupId>
                    <artifactId>*</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>org.apache.spark</groupId>
            <artifactId>spark-sql_${scala.binary.version}</artifactId>
            <version>${spark.version}</version>
            <classifier>tests</classifier>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.apache.spark</groupId>
                    <artifactId>spark-connect-shims_${scala.binary.version}</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>org.apache.spark</groupId>
            <artifactId>spark-catalyst_${scala.binary.version}</artifactId>
            <version>${spark.version}</version>
            <classifier>tests</classifier>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.apache.spark</groupId>
            <artifactId>spark-core_${scala.binary.version}</artifactId>
            <version>${spark.version}</version>
            <classifier>tests</classifier>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <executions>
                    <execution>
                        <id>shade-paimon</id>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <filters>
                                <filter>
                                    <artifact>*</artifact>
                                    <excludes>
                                        <exclude>com/github/luben/zstd/**</exclude>
                                        <exclude>**/*libzstd-jni-*.so</exclude>
                                        <exclude>**/*libzstd-jni-*.dll</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                            <artifactSet>
                                <includes combine.children="append">
                                     <include>org.apache.paimon:paimon-spark4-common_${scala.binary.version}</include>
                                </includes>
                            </artifactSet>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
